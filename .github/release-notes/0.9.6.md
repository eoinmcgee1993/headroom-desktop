Compression is back: the prefix-replay fix now keeps your provider cache AND compresses each turn. The savings chart and the Total costs saved figure also count tool-schema deferral now (tokens Headroom already saved you but never showed), so expect both to step up once after updating.
