An honest, bigger savings number: the input rate now measures compression against the input that newly enters your context window each turn. Your cached history is deliberately left untouched by Headroom, so it no longer drags the rate toward zero on long sessions. Read maturation is also on by default, which raises real savings on read-heavy sessions.
